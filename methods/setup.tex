% !TEX root = ../thesis.tex

\section{Experimental Setup}
\label{section:setup}

Here comes a description of the Setup.

\begin{figure}[t]
    \centering
    {\fontsize{7}{9}\selectfont
    \import{methods/setup/electric}{setup.pdf_tex}
    }
    \caption{Schematic overview of the experimental wiring. All the parts are mounted at the base temperature stage in the dilution fridge, except for the hf line attenuator that are mounted at the 4\,K stage (indicated with ''-10\,dB''). The \legend{seeblau65} box indicates the sample and the corresponding schema. The \legend{seeblau100} circle indicates the island, which is coupled to the MCBJ (left) and the tunnel barrier (bottom), and capacitively coupled to the DC gate (right), and the AC gate (top). Those four components are bonded via thin coppercables, that are likewise soldered to pin heads (\legend{seegrau100} circles). The \legend{seegrau65} box indicates the extra shielded part of the reference resistors ($R_\mathrm{ref}$). }
    \label{fig:methods:setup}
\end{figure}

        \subsection{cryostat and thermometer}
        \subsection{MCBJ} \label{subsec:mcbj}
            % TODO[MTH-02]: Replace this placeholder note with technical MCBJ setup details.

        \subsection{DC Cabling} \label{subsec:dc cabling}
        4point measurements
            \subsubsection*{copper powder filter}
            \subsubsection*{steel capillary cable}
            \subsubsection*{silver epoxy filter}

        \subsection{AC cabling}
            \subsubsection*{Attenuators}
            \subsubsection*{Antenna}
            \subsubsection*{stripline}
